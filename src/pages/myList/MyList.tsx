// CSS
import './MyList.css';

const MyList = (data : any) => {
    const db = data.db;
    const user = data.user;

    return (
        <>
        Hi, here is my list
        </>
    )
}

export default MyList;