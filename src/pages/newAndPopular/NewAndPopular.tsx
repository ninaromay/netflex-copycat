// CSS
import './NewAndPopular.css';

const NewAndPopular = (data : any) => {
    const db = data.db;
    const user = data.user;

    return (
        <>
        Hi, here is some new and popular media
        </>
    )
}

export default NewAndPopular;