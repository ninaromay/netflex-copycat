// CSS
import './Series.css';

const Series = (data : any) => {
    const db = data.db;
    const user = data.user;

    return (
        <>
        Here are some series
        </>
    )
}

export default Series;