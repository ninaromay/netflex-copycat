// CSS
import './ByLanguage.css';

const ByLanguage = (data : any) => {
    const db = data.db;
    const user = data.user;

    return (
        <>
        Here is some content by language
        </>
    )
}

export default ByLanguage;